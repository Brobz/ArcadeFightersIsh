TO DO:

  1. Add Some Sort of a Progression System
    -> Player Level?
    -> Currency?
    -> Runes / Masteries - like system?
    -> Customizable Skins for character / bullets?
    -> Elo?

  2. Add Lobby Customization
    -> Chat
    -> Online Matchmaking?

  3. Make Sprites and animation for everything (Maybe? Why not just shapes and colors?)

  4. Add CSS Style to the page


COMMANDS:
    Run Server Locally: node app.js
    Upload to Heroku: git add . && git commit -m "update" && git push && git push heroku master
    Open Mongo: mongo ds047146.mlab.com:47146/afi -u brobz -p astecas2012
      Then Acces AFI Database: use afi
        Insert Collection: db.createCollection("collection_name");
          Insert Documents: db.accounts.insert({username:"username", password:"password"});
